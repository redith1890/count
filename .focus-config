[16]  # Version number. Do not delete.

[[workspace]]
C:/code/count
# /home/redith/Documents/PROJECT

[ignore]
.gitignore
.git
.vscode
# .focus-config
global.focus-config
# [[build commands]]
# build_working_dir:      /home/redith/Documents/Thueland
# open_panel_on_build:    true  #  <- any settings specified here will apply to all commands unless overridden
# close_panel_on_success: false
# clear_build_output_before_running: false
# error_regex:            ^(?P<file>.*):(?P<line>\d+):(?P<col>\d+): (?P<type>error|warning): (?P<msg>.*) (\[(?P<msg1>.*)\])?$  # see examples below
# auto_jump_to_error:     false

# [Debug Build And Run]   # <- command name. Can be arbitrary
# build_command:          gcc thueland.c -o thueland  # should be an executable or a script
# build_working_dir:      /home/redith/Documents/Thueland/src
# timeout_in_seconds:     5             # if you don't want a timeout, don't specify it
# run_command:            ./thueland      # will be run if build succeeds
# run_working_dir:        /home/redith/Documents/Thueland/src      # working dir for the run command
# key_binding:            F5

# [Run]                   # <- You could have commands that don't build anything and just run something
# run_command:            ./Thueland
# run_working_dir:        /home/redith/Documents/Thueland
# key_binding:            Ctrl-F5

# [Release]
# build_command:          jai first.jai - release
# key_binding:            F9